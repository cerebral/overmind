import styled from '../../../styled-components'

export type Variant = 'header' | 'title' | 'label' | 'text'

const styles = {
  header: {
    fontSize: 'largest',
    textTransform: 'normal',
    fontWeight: 'bold',
    padding: 'large',
  },
  title: {
    fontSize: 'large',
    textTransform: 'normal',
    fontWeight: 'bold',
    padding: 'normal',
  },
  label: {
    fontSize: 'medium',
    textTransform: 'uppercase',
    fontWeight: 'bold',
    padding: 'small',
  },
  text: {
    fontSize: 'normal',
    textTransform: 'normal',
    fontWeight: 'normal',
    padding: 'small',
  },
}

export const TextElement = styled<
  {
    variant?: Variant
    dark?: boolean
  },
  'p'
>('p')`
  margin: 0;
  text-transform: ${({ theme, variant }) => styles[variant].textTransform};
  padding: ${({ theme, variant }) =>
    `${theme.padding[styles[variant].padding]} 0`};
  color: ${({ theme, dark }) => (dark ? theme.color.white : theme.color.black)};
  font-size: ${({ theme, variant }) =>
    theme.fontSize[styles[variant].fontSize]};
  font-weight: ${({ theme, variant }) => styles[variant].fontSize};
`
