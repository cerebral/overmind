import * as React from 'react'
import { Variant, TextElement } from './elements'

type Props = {
  variant: Variant
  dark?: boolean
}

const Text: React.SFC<Props> = ({ dark, variant, children }) => (
  <TextElement dark={dark} variant={variant}>
    {children}
  </TextElement>
)

export default Text
