import * as React from 'react'
import { Wrapper, Button } from './elements'
import { connect, Connect } from '../../app'
import Icon from '../common/Icon'
import { Tab } from '../../app/state'
import Tooltip from '../common/Tooltip'

const Tabs: React.SFC<Connect> = ({ appState, actions }) => (
  <Wrapper>
    <Tooltip text="State">
      <Button
        active={appState.currentTab === Tab.State}
        onClick={actions.openState}
      >
        <Icon>database</Icon>
      </Button>
    </Tooltip>
    <Tooltip text="Components">
      <Button
        active={appState.currentTab === Tab.Components}
        onClick={actions.openComponents}
      >
        <Icon>code</Icon>
      </Button>
    </Tooltip>
    <Tooltip text="Flushes">
      <Button
        active={appState.currentTab === Tab.Flushes}
        onClick={actions.openFlushes}
      >
        <Icon>save</Icon>
      </Button>
    </Tooltip>
    <Tooltip text="Console">
      <Button
        active={appState.currentTab === Tab.Console}
        onClick={actions.openConsole}
      >
        <Icon>terminal</Icon>
      </Button>
    </Tooltip>
  </Wrapper>
)

export default connect(Tabs)
