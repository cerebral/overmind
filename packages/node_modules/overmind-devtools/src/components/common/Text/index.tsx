import * as React from 'react'
import { Variant, TextElement } from './elements'
import { Colors } from '../../../theme'

type Props = {
  variant: Variant
  color?: keyof Colors
  dense?: boolean
}

const Text: React.SFC<Props> = ({ dense, color, variant, children }) => (
  <TextElement dense={dense} color={color} variant={variant}>
    {children}
  </TextElement>
)

export default Text
