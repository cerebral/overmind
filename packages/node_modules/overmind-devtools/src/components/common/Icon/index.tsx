import { h, Component } from 'overmind-components'
import * as styles from './styles'

type Props = {
  children: string
}

const Icon: Component<Props> = ({ children }) => (
  <span className={styles.icon(children as keyof typeof styles.icons)} />
)

export default Icon
