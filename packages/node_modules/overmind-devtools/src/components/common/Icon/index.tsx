import { h } from 'overmind-components'
import { Component } from '../../../app'
import * as styles from './styles'

type Props = {
  children: keyof typeof styles.icons
}

const Icon: Component<Props> = ({ children }) => (
  <span className={styles.icon(children)} />
)

export default Icon
