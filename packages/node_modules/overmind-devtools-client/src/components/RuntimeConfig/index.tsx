import { createElement } from 'overmind-components'

import { Component } from '../../overmind'
import * as styles from './styles'

const RuntimeConfig: Component = (_, { appState, actions }) => {
  return (
    <self className={styles.wrapper}>
      <div
        className={styles.overlay}
        onClick={() => actions.toggleRuntimeConfig()}
      />
      <div className={styles.contentWrapper}>
        <strong>{appState.currentApp.connectionState}</strong>
        <span style={{ margin: '0 5px' }}>on port</span>
        <strong>{appState.port}</strong>
      </div>
    </self>
  )
}

export default RuntimeConfig
