import { css } from 'emotion'
import { createElement } from 'overmind-components'

import { Component } from '../../overmind'
import { getAppShortName, nameToColor } from '../../overmind/utils'
import * as styles from './styles'

const Apps: Component = (_, { actions, store }) => {
  return (
    <self className={styles.wrapper}>
      <div
        className={styles.currentApp(nameToColor(store.currentAppName))}
        onClick={actions.toggleShowApps}
      >
        {getAppShortName(store.currentAppName)}
      </div>
      {store.showApps ? (
        <div>
          <div className={styles.overlay} onClick={actions.toggleShowApps} />
          <div className={styles.appsList}>
            {Object.keys(store.apps).map((appName) => (
              <div
                className={css(
                  styles.app,
                  store.currentAppName === appName && styles.appSelected
                )}
                key={appName}
                onClick={() => actions.selectApp(appName)}
              >
                {appName}
              </div>
            ))}
          </div>
        </div>
      ) : null}
    </self>
  )
}

export default Apps
