import { createElement } from 'overmind-components'

import { Component } from '../../overmind'
import ConsoleRow from '../ConsoleRow'
import * as styles from './styles'

const Console: Component = (_, { appState }) => (
  <self className={styles.wrapper}>
    {appState.currentApp.messages.map((message, index) => (
      <ConsoleRow key={index} message={message} />
    ))}
  </self>
)

export default Console
